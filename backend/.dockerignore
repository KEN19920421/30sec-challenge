# Dependencies (rebuilt inside the container)
node_modules

# Build output (rebuilt inside the container)
dist

# Environment files (secrets must not be baked into images)
.env
.env.*
!.env.example

# Test files
*.test.ts
*.spec.ts
tests/
coverage/
jest.config.ts
tsconfig.test.json

# Version control
.git
.gitignore

# IDE / Editor
.vscode
.idea
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Docker files (no need to copy themselves into the image)
Dockerfile
docker-compose*.yml
.dockerignore

# Logs
logs/
*.log

# Documentation
*.md
LICENSE

# Seeds (not needed in production image)
seeds/
